services:

Check failure on line 1 in .github/workflows/docker-compose.yml
GitHub Actions / .github/workflows/docker-compose.yml

Invalid workflow file

(Line: 1, Col: 1): Unexpected value 'services', (Line: 1, Col: 1): Required property is missing: jobs

  backend:
    build: ./backend
    ports: ["8000:8000"]
    
  frontend:
    build: ./frontend
    ports: ["3000:3000"]
    
  nginx:
    image: nginx:latest
    ports: ["80:80"] # The main door for the public