x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

services:
  # ===========================
  # 🧠 DATABASE (The Ledger)
  # ===========================
  db:
    image: postgres:15-alpine
    container_name: occacia-db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-admin}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-occacia_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER:-admin} -d ${DB_NAME:-occacia_db}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    logging: *default-logging

  # ===========================
  # ⚙️ BACKEND API (The Engine)
  # ===========================
  redis:
    image: redis:7-alpine
    container_name: occacia-redis
    restart: always
    logging: *default-logging

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: occacia-backend
    restart: always
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://${DB_USER:-admin}:${DB_PASSWORD}@db:5432/${DB_NAME:-occacia_db}
      DB_PASSWORD: ${DB_PASSWORD}
      SECRET_KEY: ${SECRET_KEY:?Set SECRET_KEY in .env (required)}
      ALGORITHM: ${ALGORITHM:-HS256}
      LANGFLOW_URL: ${LANGFLOW_URL}
      LANGFLOW_TOKEN: ${LANGFLOW_TOKEN}
      LANGFLOW_ORG_ID: ${LANGFLOW_ORG_ID}
      DOCKER_SOCKET: ${DOCKER_SOCKET:-/var/run/docker.sock}
      REDIS_URL: redis://redis:6379
      #SKIP_EMAIL_VERIFICATION: "true" ##Remove this after testing
    ports:
      - "8000:8000"
    logging: *default-logging

  # ===========================
  # 🌐 FRONTEND (The Face)
  # ===========================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: occacia-frontend
    restart: always
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8000 # ✅ Next.js rewrite target inside Docker
    depends_on:
      - backend
    logging: *default-logging

  # ===========================
  # 🚦 NGINX (The Traffic Cop)
  # ===========================
  nginx:
    image: nginx:alpine
    container_name: occacia-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certbot/www:/var/www/certbot:ro
      - ./certbot/conf:/etc/letsencrypt/:ro
    depends_on:
      - frontend
      - backend
    logging: *default-logging

  # ===========================
  # 📡 PORTAINER (The Radar)
  # ===========================
  portainer:
    image: portainer/portainer-ce:latest
    container_name: occacia-radar
    restart: unless-stopped
    ports:
      - "9443:9443"
    volumes:
      - ${DOCKER_SOCKET:-/var/run/docker.sock}:/var/run/docker.sock
      - portainer_data:/data
    logging: *default-logging

volumes:
  postgres_data:
  portainer_data:
